import AppShell from "@/components/AppShell";

const Connect = () => (
  <AppShell>
    <div className="container mx-auto px-4 py-16 text-center">
      <h1 className="text-3xl md:text-4xl font-bold text-[#1F3864] mb-4">Connect</h1>
      <p className="text-muted-foreground text-lg">Connect — coming in a future phase.</p>
    </div>
  </AppShell>
);

export default Connect;
