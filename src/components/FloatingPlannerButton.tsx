// Activity Planner is temporarily disabled (DPIA remediation). Hide the
// floating shortcut entirely so it does not advertise an unavailable feature.
const FloatingPlannerButton = () => null;
export default FloatingPlannerButton;

